## Rerun Log — 2025-12-13

### Environment and Code Changes
- Updated `main.py` to prefer GPU if available (`torch.cuda.is_available()`), otherwise CPU.
- Experiment 1 config set to 10 models, 800 epochs, n_jobs parallel; `save_plots=True`.
- Experiment 2 configs (delay2 0–7 cycles) set to 3 models, 400 epochs, n_jobs=3; `save_plots=True`; `BASE_PATH` and `MATLAB_PATH` pointed to `retrocueing_RNN/rerun_outputs_expt2/`.
- Patched plotting to tolerate missing subspaces and to select available example models; example loss plots use model 0.
- Moved old 1-model Exp1 figures into `figs/1_model/`.

### Experiment 1 (retrocueing_RNN/experiment_1)
- Training/eval (10 models, 800 epochs) completed; outputs at:
  `retrocueing_RNN/rerun_outputs/experiment_1/sigma0.07/kappa5.0/nrec200/lr0.0001/`
  - `saved_models/` model0–model9 (+ statedicts)
  - `training_data/` training_data_model0–9
  - `evaluation_data/` with valid/out-of-range conditions
- Analysis outputs (figs):
  - Geometry/AI: `AI_cued.*`, `PVEs_3D_cued_geometry.svg`, `cued_geometry_example_model_4.svg`, `cued_geometry_example_model_8.svg`, `psi_plane_angles_cued.svg`
  - Learning dynamics (Fig 4C style): `learning_dynamics_theta_delay{1,2}_{untrained,plateau,trained}.(png/svg)`, `learning_dynamics_AI_delay{1,2}.(png/svg)`
  - Loss plots (Fig 4A style): `fig4A_loss_model0.(png/svg)`, `example_loss_plot.(png/svg)`, `example_training_loss_and_derivative.(png/svg)`
- Pre/post-cue timepoints: pre-cue index 7 (end of delay1), post-cue index 15 (end of delay2) for Exp1 timing (stim=1, delay1=7, cue=1, delay2=7).

### Experiment 2 (retrocueing_RNN/rerun_outputs_expt2)
- Runs completed: delay2_0cycles, delay2_1cycles (3 models, 400 epochs, parallel).
  - Outputs under `.../data_vonMises/experiment_2/delay2_{0,1}cycles/sigma0.0/kappa5.0/nrec200/lr0.0001/` (saved_models, training_data, evaluation_data).
- Analysis for each stopped because Exp2 AI aggregation expects all delay variants present.
- Pending: run delay2_2cycles through delay2_7cycles with the same settings, then rerun analysis-only (no retrain/eval) on any delay config to produce Figure 5C.

### Issues Noted
- Exp2 analysis fails early when not all delay variants exist.
- Earlier runs failed when `MATLAB_PATH` pointed to placeholder; fixed by setting to `rerun_outputs_expt2/`.

### Next Actions
- Run Exp2 delay variants 2–7 with `--parallelise_flag --train_flag --eval_flag --analysis_flag` (or analysis after all evals).
- After all variants exist, rerun Exp2 analysis to generate Figure 5C.
- Optional: add guard in Exp2 analysis to skip missing variants, but best to complete all delay runs.

